services:
  web:
    image: "ghcr.io/soehlert/kitchenclip:latest"
    ports:
      - "8888:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
